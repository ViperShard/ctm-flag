# Dependencies
node_modules/

# Build output (regenerated by `npm run build`)
dist/

# Your real Firebase keys — NEVER commit. Copy firebase.config.example.js to
# this filename and fill in your values (see DEVELOPER.md).
src/firebase.config.js

# Packaged extension for teammates — contains your baked-in Firebase config.
# NEVER commit this to the public repo; share it privately (Slack/email/drive).
ctm-tag-extension.zip
*.zip

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
