/**
 * firebase.config.example.js — TEMPLATE (safe to commit; no real keys).
 *
 * SETUP: copy this file to `src/firebase.config.js`, then paste in your real
 * Firebase values. `firebase.config.js` is git-ignored so your keys stay out of
 * the public repo. Full walkthrough in DEVELOPER.md.
 *
 *   cp src/firebase.config.example.js src/firebase.config.js
 *   # then edit src/firebase.config.js with your apiKey / authDomain / projectId
 */
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
};
